import { AutoHeight } from './auto-height.directive';

describe('AutoTextResize', () => {
  it('should create an instance', () => {
    const directive = new AutoHeight();
    expect(directive).toBeTruthy();
  });
});
